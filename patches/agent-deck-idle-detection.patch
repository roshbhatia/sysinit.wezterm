--- a/plugin/init.lua
+++ b/plugin/init.lua
@@ -488,7 +488,8 @@ local function detect_status(pane, agent_type, config)
         local trimmed = line:match('^%s*(.-)%s*$') or ''

         -- Check for ">" prompt (Claude/OpenCode prompt starts with >)
-        if trimmed == '>' or trimmed:match('^>%s') then
+        -- Check for bare ">" idle prompt only; "> text" lines are Claude Code tool output
+        if trimmed == '>' then
             return 'idle'
         end
